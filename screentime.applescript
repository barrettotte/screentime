# Invoke ScreenTime swift application; This gets compiled to a MacOS application.

do shell script "swift run --package-path ~/code/barrettotte/swift/screentime"

# test with osascript screentime.applescript